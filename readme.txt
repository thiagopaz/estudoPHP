Orientação Objetos :

- Classe é como se fosse um esboço .
- O objeto é uma instancia de uma classe .

Exemplo de como instanciar : 
 $car = new Car(); //Car é nome do carro. 

Trait : É para numa classe usar o método da TRAIT sem precisar instanciar um objeto de uma classe pois nossas classes 
        já adquiriram as características(métodos e atributos) da Trait;
    Exemplo : no arquivo trait.php


